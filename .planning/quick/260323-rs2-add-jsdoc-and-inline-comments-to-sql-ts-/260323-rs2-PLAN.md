---
phase: quick
plan: 260323-rs2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sql.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every exported type and function in sql.ts has a JSDoc comment explaining WHAT it represents"
    - "The tableProxyHandler proxy has inline comments explaining the purpose of each case branch"
    - "The createSQLContext function and its returned object have JSDoc explaining the SQL context API"
    - "The sqlTaggedTemplate function has JSDoc and inline comments for each parameter-type case"
    - "No trivial comments were added to self-documenting code"
  artifacts:
    - path: "src/sql.ts"
      provides: "JSDoc and inline comments for the SQL template literal system"
      contains: "createSQLContext"
  key_links:
    - from: "src/sql.ts"
      to: "src/database.ts"
      via: "createSQLContext import and SQLTemplate/SQLQuery exports"
      pattern: "export.*(SQLTemplate|SQLQuery|createSQLContext)"
---

<objective>
Add JSDoc and inline comments to src/sql.ts — the type-safe SQL template literal engine.

Purpose: sql.ts is the most complex file in the codebase (950 lines of nested Proxy handlers, curried format functions, and advanced TypeScript mapped types). It has zero documentation. Adding WHAT-focused JSDoc and inline comments makes it maintainable.

Output: Fully documented sql.ts with JSDoc on all exports/types and inline comments on non-trivial proxy logic.
</objective>

<execution_context>
@/workspaces/oopg/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/oopg/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/sql.ts
@src/database.ts (lines 450-475 for createSQLContext usage, lines 77-82 for SQLQuery type)
@src/types.ts (EntityDescription, PGType, JSType, Row)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSDoc to exported types and the sqlTaggedTemplate engine</name>
  <files>src/sql.ts</files>
  <action>
Add JSDoc comments to the following exported types and key internal functions in src/sql.ts. Comments explain WHAT each thing represents, not HOW the code works line-by-line.

**Exported types (lines 27-40):**
- `SQLTemplate` — Tagged template function type returned by `createSQLContext`. Accepts a template literal with interpolated values and returns `{ sql, values }` for parameterized queries.
- `SQLCTEDefinition` — Represents a Common Table Expression that can be aliased and materialized.
- `SQLCTE` — Tagged template function that creates a CTE definition from a SQL fragment.
- `SQLQuery` (line 177) — The core query shape: `{ sql: string; values: unknown[] }`.

**Internal mapped types (lines 42-162) — add concise single-line or 2-line JSDoc:**
- `SQLTables` — Maps a Database class's schema entities (tables, views, functions, sub-schemas) to their SQL-template counterparts. This is the type of `tables` in `db.sql`.
- `SQLTable` — SQL template representation of a table: provides `$insert`, `$update`, plus column/select accessors from `SQLSelect`.
- `SQLSelect` — SQL template representation of a selectable entity (table or view): provides `$all`, `$select`, `$columns`, and per-column accessors.
- `SQLFunction` — SQL template representation of a stored function: callable with typed args, returns an aliasable SQL fragment.
- `SQLFunctionParams` — Maps PGType argument definitions to their JavaScript equivalents for function call parameters.
- `SQLParam` — Union of types accepted as SQL template parameters.
- `SQLColumn` — SQL template representation of a single column: supports aliasing, casting, and JSON field access.
- `SQLAlias` — Makes a value both directly usable and callable-with-alias: `T & ((alias?: string) => T)`.
- `SQLFormat` — Adds a `$format(options)` method to any SQL fragment for controlling quoting/schema/table prefixing.
- `SQLCast` — Adds a `$cast(type)` method to any SQL fragment for PostgreSQL type casting.

**Key internal constants (lines 164-176):**
- Add a brief inline comment above the Symbol declarations explaining that `CallableProp` marks proxy-interceptable callables, `SQLProp` carries the generated SQL string on format objects, and `ValuesProp` carries parameter values.

**`isQuery` (line 179):**
- JSDoc: Type guard that checks whether a value is an SQLQuery (has `sql` and `values` properties).

**`sqlTaggedTemplate` (line 188):**
- JSDoc: Core template literal handler. Processes a tagged template string, converting interpolated values into parameterized SQL (`$1`, `$2`, ...) with proper type-specific casting. Returns an SQLQuery that is also callable as `(alias) => SQLQuery` for use as subqueries.
- Add inline comments for each case in the switch statement (lines 203-254) — keep them short (one line each), explaining WHAT the case handles:
  - null case: "Null literal — pass through as parameterized value"
  - SQLProp case: "SQL fragment from a format function — inline the pre-built SQL string"
  - isQuery case: "Subquery — renumber its $N placeholders to avoid collisions and merge values"
  - boolean case: "Boolean — cast explicitly to avoid implicit pg coercion"
  - Date case: "Date — convert to ISO string with timestamptz cast"
  - number case: "Number — cast to numeric"
  - string/array case: "String or array — pass as-is (pg handles binding)"
  - object case: "Plain object — serialize to JSON with jsonb cast"

Do NOT add comments to the format helper functions (formatTable, formatColumn, formatColumns, etc. lines 267-472) unless something is truly non-obvious. These are straightforward SQL string builders.
  </action>
  <verify>
    <automated>cd /workspaces/oopg && pnpm exec biome check src/sql.ts --no-errors-on-unmatched && pnpm build</automated>
  </verify>
  <done>All exported types and sqlTaggedTemplate have JSDoc. Switch cases have inline comments. Build passes. No trivial comments on obvious format helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add JSDoc and inline comments to proxy handlers and createSQLContext</name>
  <files>src/sql.ts</files>
  <action>
Add JSDoc and inline comments to the proxy-heavy sections of sql.ts: `tableProxyHandler`, `createSQLContext`, and the `callable` helper.

**`callable` (line 487):**
- JSDoc: Marks a function as proxy-interceptable by attaching the `CallableProp` symbol. When the SQL template engine encounters a callable during interpolation, it invokes it to get the SQL fragment before processing.

**`tableProxyHandler` (line 494):**
- JSDoc: Proxy handler that intercepts property access on table/view references inside SQL templates. Each property access on a table (e.g., `tables.users.name`) resolves to a formatted SQL column reference. Special `$`-prefixed properties (`$insert`, `$update`, `$format`, `$select`, `$columns`, `$all`) provide table-level SQL operations.
- Add inline comments for each major case in the switch (lines 498-687):
  - `$insert`: "Returns an INSERT target: `schema.table (col1, col2)` with optional column list"
  - `$update`: "Returns SET clause fragments: `col1 = $1, col2 = $2` from a key-value object"
  - `$format`: "Returns the table's formatted SQL name with configurable schema/quoting"
  - `$select`: "Returns a column list from selected column names with optional aliases"
  - `$columns`: "Returns a column list without table prefix (for INSERT column lists)"
  - `$all`: "Returns `table.*` or just `*` depending on format options"
  - `SQLProp`/`ValuesProp`/`CallableProp`: "Internal symbol access — pass through to underlying object"
  - default (line 544): "Column access — returns a proxy that resolves to a formatted column reference. The returned value is both callable (for aliasing: `col('alias')`) and a proxy itself (for JSON field access: `col.field` and casting: `col.$cast('int4')`)"
- Inside the nested column proxy handler (line 581), add a single block comment before the switch:
  "Handles sub-properties of a column reference: `$cast` for type casting, `$format` for output control, and string keys for JSON field access (`column->>'field'`)."

**`createSQLContext` (line 690):**
- JSDoc: Creates the SQL context object used by `Database.sql()` template literals. Returns `{ sql, tables, utils }` where:
  - `sql` is the tagged template handler for building parameterized queries
  - `tables` is a type-safe proxy that maps database schema entities to SQL fragments
  - `utils` provides helper functions (`raw`, `table`, `cte`, `type`, `and`, `array`)
- Add inline comments inside the tablesProxy handler (lines 694-833) for each major case:
  - isFunc case (line 701): "Database function — return a callable that produces `schema.func_name(args)` SQL"
  - isSchema case (line 735): "Sub-schema — return a nested proxy that resolves schema.entity references"
  - isTable/isView case (line 809): "Table or view — wrap in tableProxyHandler for column/operation access"
  - non-strict fallback (line 812): "Loose mode — treat unknown properties as untyped table references"
- Add JSDoc to each `utils` method (lines 841-947):
  - `raw`: "Injects a raw SQL string without parameterization. Use with caution — values are NOT escaped."
  - `table`: "Creates a table proxy from an EntityDescription, enabling column access outside the schema."
  - `cte`: "Defines a Common Table Expression (WITH clause). Returns a proxy with `.alias()`, `.use()`, and column accessors."
  - `type`: "Returns a SQL type cast fragment (e.g., `int4`, `text[]`) for use in template interpolation."
  - `and`: "Joins multiple SQLQuery fragments with AND, renumbering parameter placeholders to avoid collisions."
  - `array`: "Converts a JavaScript array to a `jsonb` literal for use in SQL templates."

Do NOT over-comment. The curried format functions (tableFormatFunction, columnListFunction, insertFormatFunction, columnFormatFunction, columnFieldFormatFunction) are internal plumbing — skip them unless something is genuinely surprising.
  </action>
  <verify>
    <automated>cd /workspaces/oopg && pnpm exec biome check src/sql.ts --no-errors-on-unmatched && pnpm build</automated>
  </verify>
  <done>tableProxyHandler, createSQLContext, and utils all have JSDoc. Proxy switch cases have inline comments explaining WHAT each branch resolves to. Build passes. No comments on obvious format plumbing.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds (no type errors introduced by comments)
- `pnpm exec biome check src/sql.ts` passes (no lint issues)
- Every `export type` and `export const/function` in sql.ts has a JSDoc block
- Proxy handler branches have inline comments explaining WHAT they resolve to
- No trivial comments on self-documenting format helper functions
</verification>

<success_criteria>
- sql.ts has JSDoc on all 12+ exported/key types and 3 major functions
- The two proxy handlers (tableProxyHandler, tablesProxy in createSQLContext) have inline comments on each switch case
- sqlTaggedTemplate switch cases have inline comments
- Build and lint pass without errors
- No comments added to obvious code (format string builders, simple variable assignments)
</success_criteria>

<output>
After completion, create `.planning/quick/260323-rs2-add-jsdoc-and-inline-comments-to-sql-ts-/260323-rs2-SUMMARY.md`
</output>
