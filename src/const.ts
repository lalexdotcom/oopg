/** Default PostgreSQL schema name used when no schema is specified (`'public'`). */
export const DEFAULT_POSTGRES_SCHEMA = 'public';

/** Default PostgreSQL port number (`5432`). */
export const DEFAULT_POSTGRES_PORT = 5432;
